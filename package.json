{
  "name": "easydpi",
  "version": "1.1.1",
  "description": "The default template for a Neutralinojs app. It's possible to use your favorite frontend framework by using [these steps](https://neutralino.js.org/docs/getting-started/using-frontend-libraries).",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "npx @neutralinojs/neu build",
    "tag": "powershell -Command \"$ver = (Get-Content package.json | ConvertFrom-Json).version; git tag v$ver; git push origin v$ver\"",
    "release": "standard-version",
    "release:full": "npm run release && git push --follow-tags origin master"
  },
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
